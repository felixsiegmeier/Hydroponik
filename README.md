# Hydroponik

Dies ist der Code für eine kleine Hydroponikanlage.

ToDo:
* EC-Sensor Aufbau und ggf. Code anpassen
    * GND an GND, VIN an 5V und dann über mosfet steuern?
        * alternativ GND an Pin und den dann an/aus schalten?

für den EC-Sensor bräuchte ich einen P-Kanal-Mosfet, um (+) zu schalten

=> vlt. ist es sinnvoll, für den EC-Sensor und für die Pumpe statt eines MOSFETS ein Relais zu verwenden!?